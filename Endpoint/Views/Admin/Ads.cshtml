@{

    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    int x = 1;
}
@model List<Ads>
<div class="container">
    <div class="row">
        <form asp-action="AddAds" method="post">
            <div class="row">
                <div class="col-md-5">
                    <label for="Title" style="font-size:20px">آدرس آگهی :</label>
                    <input type="text" class="form-control" id="Name" name="Name">
                </div>
                <div class="col-5">
                    <br>
                    <button type="submit" class="btn btn-success "> ثبت آگهی </button>
                </div>
            </div>

        </form>
    </div>
    <div class="row">
        <h2>لیست آگهی ها</h2>
        <table class="table-fluid table-bordered table-hover table-condensed table-striped">
            <thead>
                <tr>
                    <th>ردیف</th>
                    <th>شناسه آگهی</th>
                    <th> محتوا</th>
                    <th>عملیات</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@(x++)</td>
                        <td>@item.AdsId</td>
                        <td><img style="width:100px" src="@item.Url"></td>
                        <td>
                            <div class="container">
                                <div class="row">

                                    <div class="col-md-6"><a href="~/admin/DeleteAds/@item.AdsId" class="btn btn-sm btn-danger">حذف</a></div>
                                </div>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

    </div>
</div>
<hr />

